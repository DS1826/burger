<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
		{{#if devour}}Devour It!{{else}}?Button!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Delete da Burger!</button>
</li>